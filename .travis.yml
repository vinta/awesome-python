language: python

python:
  - "3.6"

before_script:
  - pip install mkdocs mkdocs-material

script:
  - cp README.md docs/index.md
  - mkdocs build
  
deploy:
  provider: pages
  local-dir: "site"
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master
